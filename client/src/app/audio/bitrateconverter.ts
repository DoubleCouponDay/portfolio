
class _bitrateconverter {
    convertpcmtowebaudio(chunk: number[]) {
        chunk.forEach((value, index) => {
            
        })
    }
}

export const bitrateconverter = new _bitrateconverter()