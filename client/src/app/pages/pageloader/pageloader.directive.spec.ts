import { PageloaderDirective } from "./pageloader.directive";

describe('PageloaderDirective', () => {
  it('should create an instance', () => {
    const directive = new PageloaderDirective();
    expect(directive).toBeTruthy();
  });
});
